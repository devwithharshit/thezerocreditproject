{% if section.settings.show_announcement and section.settings.text != blank %}
  <aside
    class="announcement-bar"
    aria-label="Announcement"
    style="--announcement-background: {{ section.settings.background }}; --announcement-text: {{ section.settings.text_color }}; --announcement-duration: {{ section.settings.speed }}s;"
  >
    <div class="announcement-bar__viewport">
      <div class="announcement-bar__track{% if section.settings.animate %} is-animated{% endif %}">
        {% for repeat in (1..6) %}
          <div class="announcement-bar__item" aria-hidden="{% if repeat == 1 %}false{% else %}true{% endif %}">
            {% if section.settings.link != blank %}
              <a href="{{ section.settings.link }}">{{ section.settings.text | escape }}</a>
            {% else %}
              <span>{{ section.settings.text | escape }}</span>
            {% endif %}
            <span class="announcement-bar__dot" aria-hidden="true"></span>
          </div>
        {% endfor %}
      </div>
    </div>
  </aside>
{% endif %}

{% schema %}
{
  "name": "Announcement bar",
  "settings": [
    {
      "type": "checkbox",
      "id": "show_announcement",
      "label": "Show announcement",
      "default": true
    },
    {
      "type": "text",
      "id": "text",
      "label": "Text",
      "default": "Made to order. Built from zero."
    },
    {
      "type": "url",
      "id": "link",
      "label": "Link"
    },
    {
      "type": "checkbox",
      "id": "animate",
      "label": "Animate announcement",
      "default": false
    },
    {
      "type": "range",
      "id": "speed",
      "label": "Animation speed",
      "min": 20,
      "max": 60,
      "step": 5,
      "unit": "s",
      "default": 55
    },
    {
      "type": "color",
      "id": "background",
      "label": "Background",
      "default": "#111111"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#ffffff"
    }
  ]
}
{% endschema %}
